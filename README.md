# llm-tools

This is a repo of simple tools written fully by llms. The tools are single html files (with simple vanilla js and css) that are deployed on github pages. `index.html` is the main entry point. That lists all the tools. 